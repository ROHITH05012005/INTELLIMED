# INTELLIMED Frontend Environment Variables
# Copy this file to .env and fill in your values

# Backend API Base URL
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Use mock data instead of real API (set to "true" during development)
VITE_USE_MOCK=true

# App environment
VITE_APP_ENV=development
